# Build, runtime and dependency caches
.next
apps/web/.next
out
build
dist
.turbo
node_modules
pnpm-lock.yaml
package-lock.json
next-env.d.ts
apps/web/next-env.d.ts
coverage
*.log
*.tmp
.temp*
.git
.gemini

# Unsupported syntax without native Prettier parser
prisma/schema.prisma
*.toml

# Scripts governed by external/shell validators
scripts/ai/

# Specification documents, contracts and templates governed by dedicated Python and JSON schema validators
docs/
.github/ISSUE_TEMPLATE/
.github/PULL_REQUEST_TEMPLATE.md
.github/workflows/product-spec-validation.yml
.github/workflows/feature-contract-gate.yml
# Generated artifact. render-static.js rewrites it wholesale, so any hand
# formatting is undone on the next run and the file would sit permanently
# unformatted-after-generation. It is checked in to be read, not edited.
DASHBOARD.html

# The unparsable fixture carries a NUL byte on purpose: it is what proves the
# secret-surface guard fails closed on a file it cannot read (AC-AI-18-06).
# Prettier cannot parse it, and making it parsable would delete the fixture.
tools/ai-guard/test/fixtures/unparsable-source/
