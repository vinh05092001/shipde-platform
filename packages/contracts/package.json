{
  "name": "@shipde/contracts",
  "version": "0.1.0",
  "description": "Ship Dễ — Shared domain contracts, API shapes, and schema definitions",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@shipde/config": "workspace:*",
    "typescript": "^5"
  }
}
