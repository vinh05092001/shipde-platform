# @shipde/config

Shared workspace configuration profiles for TypeScript, linting, and toolchains across the Ship Dễ monorepo.

## Purpose

- Centralize TypeScript base compiler options (`tsconfig.base.json`).
- Provide shared environment and build-time constants.

## Boundary

- Contains reusable base configuration and type declarations.
- Does not contain runtime business logic or UI components.
